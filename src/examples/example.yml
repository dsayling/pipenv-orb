description: |
  Using pipenv-orb
usage:
  version: 2.1
  orbs:
    pipenv: circleci/pipenv@Alpha
  workflows:
    main:
      jobs:
        - build
  jobs:
    build:
      executor: pipenv/default
      steps:
        - checkout
        # - pipenv/load-cache
        - pipenv/install
        # - pipenv/save-cache
        - run:
            name: "Test"
            command: |
              # pytest would be a dep in Pipfile
              pytest
